// Campaign config — populated in Task 2. All values that change over time
// (Meshulam link, contact info, raised amount, tier amounts) live here so
// the rest of the page never needs to be touched for routine updates.
const CONFIG = {};
